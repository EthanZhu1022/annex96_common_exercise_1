\chapter{Method and Progress}\label{ch:method}

This chapter describes what has been implemented and what has been completed by
the midterm stage. The main training pipeline now runs end-to-end, the selected
Vermont benchmark experiments have completed their planned training budgets, and
the evaluation utilities export unified test-month metrics, per-building
temperature tracking figures, ranking tables, and load-tracking figures. The
project can now compare several baselines and communication variants within the
same MAPPO framework using the completed Vermont result set.

中文：本章介绍当前已经实现的方法，以及中期阶段已经完成的工作。
目前主训练流程已经可以完整运行，选定的 Vermont 基准实验也已经按计划训练完成，
评估工具能够统一导出测试月指标、每栋建筑的温度跟踪图、排序表和负荷跟踪图。
此项目已可以在同一个 MAPPO 框架下对多个基线和通信变体进行完整比较。


\section{Problem Definition}

The CityLearn Annex 96 task is treated as a cooperative control problem with
$N=25$ building agents. At time step $t$, building $i$ observes its own local
state $o_i^t$ and chooses a continuous action $a_i^t$ for its controllable
devices. The actions of all buildings together affect battery states, indoor
conditions, and the total portfolio load. The goal is to make the total load
follow a daily reference profile $r_t$ without creating too many comfort
violations or other negative side effects.

中文：CityLearn Annex 96 任务在本项目中被看作一个合作型控制问题。
任务中有 $N=25$ 个建筑智能体。
在时间步 $t$，建筑 $i$ 看到自己的本地状态 $o_i^t$，并为可控设备选择连续动作 $a_i^t$。
所有建筑的动作共同影响电池状态、室内状态和整体建筑组合负荷。
目标是让总负荷跟踪每日参考曲线 $r_t$，同时不要造成过多舒适性违背或其他负面影响。


The project objective is not only to get a high training reward. In practice,
the controller should reduce the testing-month tracking error, measured by NMBE
and CV-RMSE, while keeping comfort, ramping, peak demand, and fairness in a
reasonable range. Cost and carbon emissions are part of the Annex 96 secondary
metric set, but the CE1 schemas used here do not provide pricing or
carbon-intensity time series, so those two metrics are not interpreted in the
current experiments. This is why the method uses a centralized critic and
structured communication between actors.

中文：本项目的目标不只是让训练 reward 变高。
更实际的目标是在测试月份降低负荷跟踪误差，也就是降低 NMBE 和 CV-RMSE，同时让舒适性、爬坡、峰值负荷和公平性保持在合理范围内。
成本和碳排放属于 Annex 96 次级指标的一部分，但本项目使用的 CE1 schema 没有提供电价或碳排放强度时间序列，
因此当前实验不解释这两个指标。
这也是本项目使用集中式 critic 和 actor 间结构化通信的原因。


\section{Environment and Baseline Pipeline}

The first completed component is the environment interface. CityLearn is wrapped
for several control and learning pipelines:

中文：首先完成的是环境接口。
本项目为多个控制和学习流程封装了 CityLearn 环境：


\begin{itemize}
  \item RBC notebook baseline for non-learning comparison;
  中文：用于非学习方法对比的 RBC notebook 基线；
  \item independent PPO and independent SAC baselines, where each building
  learns without communication;
  中文：独立 PPO 和独立 SAC 基线，其中每栋建筑在没有通信的情况下学习；
  \item standard MAPPO, where all buildings use a shared policy and a
  centralized critic;
  中文：标准 MAPPO，其中所有建筑使用共享策略和集中式 critic；
  \item grouped MAPPO, where clusters of buildings share group-specific actors;
  中文：分组 MAPPO，其中同一组建筑共享组内 actor；
  \item communication-aware grouped MAPPO, where a communication layer is
  inserted between actor encoders and action heads.
  中文：带通信的分组 MAPPO，其中通信层被插入 actor 编码器和动作头之间。
\end{itemize}

For the current standard, grouped, and communication-aware MAPPO experiments,
the \texttt{mappo\_standard/env.py} wrapper returns local observations,
centralized observations, continuous action spaces, rewards, done masks, and
episode lengths in the tensor format expected by \texttt{on-policy-main}. The
training scripts then reuse \texttt{R\_MAPPO}, \texttt{R\_MAPPOPolicy}, and
\texttt{SharedReplayBuffer} from that implementation, instead of reimplementing
the PPO optimizer and buffer logic. This reduces implementation risk and keeps
the research focus on CityLearn adaptation, grouping, and communication.

中文：对于当前的标准 MAPPO、分组 MAPPO 和带通信 MAPPO 实验，
\texttt{mappo\_standard/env.py} 中的封装会返回局部观测、集中式观测、连续动作空间、奖励、done mask 和 episode 长度，并把它们整理成 \texttt{on-policy-main} 所需要的张量格式。
随后训练脚本直接复用该实现中的 \texttt{R\_MAPPO}、\texttt{R\_MAPPOPolicy} 和 \texttt{SharedReplayBuffer}，而不是重新实现 PPO 优化器和 buffer 逻辑。
这样可以降低实现风险，使研究重点集中在 CityLearn 适配、建筑分组和通信设计上。


\section{Grouped Actor Architecture}

The grouped actor design sits between two simple choices. If all buildings share
one actor, training is efficient, but the policy may ignore important
differences between buildings. If every building has its own actor, the policy
can be more flexible, but it needs more data and coordination becomes harder.
The grouped design shares an actor only within similar buildings.

中文：分组 actor 设计位于两种简单方案之间。
如果所有建筑共享一个 actor，训练效率高，但可能忽略建筑之间的重要差异。
如果每栋建筑都有自己的 actor，策略更灵活，但需要更多数据，协同也更难。
因此当前方案只让相似建筑共享同一个 actor。


Buildings are grouped with K-means using features related to flexibility, mainly
battery energy storage capacity and total HVAC capacity. After clustering,
buildings in group $G_k$ share the same actor network. All groups still share one
centralized critic. The critic sees the full portfolio state, while each actor
uses its local building observations and, when enabled, communication-enhanced
features.

中文：建筑使用 K-means 根据灵活性相关特征进行分组，主要包括电池储能容量和 HVAC 总容量。
聚类后，组 $G_k$ 中的建筑共享同一个 actor 网络。
所有组共享一个集中式 critic。
critic 可以看到完整的建筑组合状态，而每个 actor 使用本地建筑观测，以及在启用通信时得到的通信增强特征。


This architecture has been implemented in the \texttt{mappo\_grouped} package.
The same grouping information is reused by communication-aware variants so that
communication effects can be compared against a consistent grouped baseline.

中文：该架构已经在 \texttt{mappo\_grouped} 包中实现。
带通信的变体会复用同一套分组信息，这样通信效果就可以和一致的分组基线进行比较。


\section{MAPPO Learning Structure}

The learning structure follows centralized training and decentralized
execution. During training, the critic can see the full portfolio state and
learn whether the overall control decision is good. During execution, each
group actor only uses its own building observations, plus any communication
features, to choose continuous actions. After each episode, MAPPO uses the
collected experience to estimate which actions were better than expected, and
then updates the actors with PPO's conservative update rule.

中文：学习结构遵循集中训练、分散执行。
训练时，critic 可以看到整个建筑组合的状态，用来判断整体控制效果好不好。
执行时，每个组的 actor 只根据本组建筑的观测，以及可能加入的通信特征，选择连续动作。
每个 episode 结束后，MAPPO 会根据这一轮收集到的经验判断哪些动作比预期更好，然后用 PPO 中比较稳健的更新方式调整 actor。


The grouped replay buffer keeps the group-agent structure when samples are drawn
for PPO updates. This matters for communication: the communication layer needs
to know which agents were in the same group at the same time step. If the data is
flattened too early, that structure is lost. The buffer therefore keeps tensors
in a shape that supports both MAPPO updates and group communication.

中文：分组 replay buffer 在 PPO 更新采样时保留组内智能体结构。
这对通信很重要：通信层需要知道同一时间步里哪些智能体属于同一组。
如果过早把数据压平成普通 batch，这个结构就丢失了。
因此，当前实现让张量保持一种既支持 MAPPO 更新、又支持组内通信的形状。


\section{Baseline and Communication Variants}

The implemented methods are organized as a step-by-step experimental path. The
project first tests individual learning methods, then standard MAPPO, then
grouped MAPPO without communication, and finally several communication variants.
This order makes it easier to see what is added at each stage.

中文：这一部分按照实验推进顺序来组织。
项目先测试个体学习方法，再测试标准 MAPPO，然后测试无通信的分组 MAPPO，最后再加入不同通信模块。
这样写可以更清楚地说明每一步相比前一步新增了什么。


\subsection{Individual PPO and SAC Baselines}

The first learning baselines are independent PPO and independent SAC. In these
runs, each building learns without receiving information from other buildings.
This gives a simple reference point for what can be achieved without multi-agent
coordination.

中文：第一类学习基线是独立 PPO 和独立 SAC。
在这些实验中，每栋建筑在不接收其他建筑信息的情况下学习。
这提供了一个简单参照，用来观察在没有多智能体协同的情况下大概能做到什么水平。


\subsection{Standard MAPPO}

The next step is standard MAPPO. All 25 buildings reuse the same actor network
through parameter sharing, while the centralized critic sees the full portfolio
state during training. There is still no explicit communication module in this
baseline. It tests whether a basic MARL method already improves the task before
adding grouping or communication design.

中文：下一步是标准 MAPPO。
25 栋建筑通过参数共享复用同一个 actor 网络，训练时集中式 critic 可以看到完整的建筑组合状态。
这个基线里仍然没有显式通信模块。
它用来测试：在还没有加入分组和通信设计之前，基础 MARL 方法本身是否已经能改善任务效果。


\subsection{Grouped MAPPO Without Communication}

After standard MAPPO, buildings are divided into groups with K-means. Buildings
in the same group share one group actor, but the encoded feature is not changed
by any message passing:
\[
\tilde{h}_i = h_i .
\]
This tests whether grouping alone helps. It is also the direct no-communication
baseline for later communication modules.

中文：在标准 MAPPO 之后，建筑会通过 K-means 被分成不同组。
同一组建筑共享一个组内 actor，但编码后的特征不会被任何消息传递改变：
\[
\tilde{h}_i = h_i .
\]
这一步用来测试单独分组是否有帮助。
它也是后续通信模块最直接的无通信对照。


In the communication variants below, the actor first encodes each building
observation into a hidden feature. The communication layer then modifies this
feature before the action head selects the final action:
\[
o_i \rightarrow \mathrm{encoder}_k \rightarrow h_i
\rightarrow \mathrm{communication} \rightarrow \tilde{h}_i
\rightarrow \mathrm{action\ head}_k \rightarrow a_i .
\]
Because the communication module is part of the actor network, it is trained
together with the actor.

中文：在下面的通信变体中，actor 会先把每栋建筑的观测编码成隐藏特征。
通信层会在动作头选择最终动作之前，对这个隐藏特征进行更新：
\[
o_i \rightarrow \mathrm{encoder}_k \rightarrow h_i
\rightarrow \mathrm{communication} \rightarrow \tilde{h}_i
\rightarrow \mathrm{action\ head}_k \rightarrow a_i .
\]
因为通信模块是 actor 网络的一部分，它会和 actor 一起训练。


\subsection{CommNet-Style Mean Communication}

For each group, the CommNet-style module lets each agent receive the average
hidden feature of the other agents:
\[
m_i = \frac{1}{|G_k|-1}\sum_{j \in G_k, j \ne i} h_j .
\]
This average message is passed through a small neural network and then added
back to the agent's own feature:
\[
\tilde{h}_i = h_i + f_\theta(m_i).
\]
This is the simplest communication baseline in the project.

中文：对于每个组，CommNet 风格模块让每个智能体接收其他智能体隐藏特征的均值：
\[
m_i = \frac{1}{|G_k|-1}\sum_{j \in G_k, j \ne i} h_j .
\]
这个平均消息会经过一个小型神经网络，然后加回智能体自己的特征：
\[
\tilde{h}_i = h_i + f_\theta(m_i).
\]
这是本项目中最简单的通信基线。


\subsection{Weighted Same-Group and Other-Group Communication}

The project-specific communication module separates messages from similar
buildings and messages from the rest of the portfolio. For group $G_k$,
\[
m^{same}_k = \frac{1}{|G_k|}\sum_{j \in G_k} h_j,
\qquad
m^{other}_k = \frac{1}{N-|G_k|}\sum_{j \notin G_k} h_j .
\]
The combined message is
\[
m_k = \alpha m^{same}_k + \beta m^{other}_k,
\qquad \alpha > \beta \geq 0.
\]
The default implementation uses $\alpha=0.75$ and $\beta=0.25$. This means that
same-group information is weighted more strongly, while other-group information
is still kept as weaker global context.

中文：本项目设计的通信模块将相似建筑的信息和其他建筑的信息分开处理。
对于组 $G_k$：
\[
m^{same}_k = \frac{1}{|G_k|}\sum_{j \in G_k} h_j,
\qquad
m^{other}_k = \frac{1}{N-|G_k|}\sum_{j \notin G_k} h_j .
\]
组合后的消息为：
\[
m_k = \alpha m^{same}_k + \beta m^{other}_k,
\qquad \alpha > \beta \geq 0.
\]
默认实现使用 $\alpha=0.75$ 和 $\beta=0.25$。
这表示组内信息权重更高，但组外信息仍然作为较弱的全局背景保留下来。


\subsection{PowerNet-Style Structured Information Passing}

The PowerNet-style module tests a more structured way of sharing information. It
can use ring, chain, or full topologies to decide which other agents are included
in the message. For adjacency matrix $A$, the selected peer message is
\[
m_i = \frac{1}{\max(1,\sum_j A_{ij})}\sum_j A_{ij}g_\theta(h_j).
\]
The implementation then combines the agent's own feature with the peer message
using a learned residual update. The purpose is to avoid overwriting local
information while still allowing agents to coordinate.

中文：PowerNet 风格模块测试的是更结构化的信息共享方式。
它可以使用 ring、chain 或 full 拓扑来决定哪些其他智能体会被纳入消息。
对于邻接矩阵 $A$，被选择的其他智能体消息为：
\[
m_i = \frac{1}{\max(1,\sum_j A_{ij})}\sum_j A_{ij}g_\theta(h_j).
\]
当前实现随后把智能体自己的特征和其他智能体消息结合起来，并通过学习到的残差更新生成新特征。
这样做的目的是尽量不覆盖本地信息，同时仍然让智能体能够协同。


\subsection{GAT-Style Graph Attention Communication}

The GAT-style module uses masked graph attention over a fixed similarity graph.
Inside each K-means group, buildings are fully connected, and a small number of
cross-group edges connect the closest groups. Each agent then learns attention
weights over its allowed neighbors instead of using a simple average. This makes
it possible to test whether graph-structured attention improves communication
selectivity in the CityLearn setting.

中文：GAT 风格模块在一个固定的相似性图上使用掩码图注意力。
在每个 K-means 组内，建筑之间是全连接的，同时还会用少量跨组边连接最相近的组。
这样每个智能体不再只做简单平均，而是会对允许连接的邻居学习不同注意力权重。
这个模块用于测试：在 CityLearn 任务中，图结构注意力能否提高通信选择性。


\subsection{TarMAC-Style Attention}

The TarMAC-style module uses attention. Each agent produces query, key, and
value features, and the attention weights are
\[
\alpha_{ij} =
\frac{\exp(q_i^\top k_j / \sqrt{d})}
{\sum_l \exp(q_i^\top k_l / \sqrt{d})},
\]
and the received context is
\[
c_i = \sum_j \alpha_{ij}v_j .
\]
The updated feature is computed from $h_i$ and $c_i$. Compared with the fixed
$\alpha$ and $\beta$ weights above, this module learns the communication weights
from the current features. It tests whether the agents can learn which other
agents are worth paying more attention to.

中文：TarMAC 风格模块使用注意力机制。
每个智能体都会生成 query、key 和 value 特征。
注意力权重为：
\[
\alpha_{ij} =
\frac{\exp(q_i^\top k_j / \sqrt{d})}
{\sum_l \exp(q_i^\top k_l / \sqrt{d})},
\]
接收到的上下文为：
\[
c_i = \sum_j \alpha_{ij}v_j .
\]
更新后的特征由 $h_i$ 和 $c_i$ 共同计算得到。
和上面固定的 $\alpha$、$\beta$ 权重相比，这个模块会根据当前特征学习通信权重。
该模块用于测试智能体能否学会更应该关注哪些其他智能体。


\subsection{DIAL-Style Differentiable Communication}

The DIAL-style module tests a different communication idea. Each agent first
turns its hidden feature into a message. During training, the message remains
continuous and differentiable, so gradients can pass through the communication
channel. During evaluation, the message can be discretized. In this project, the
DIAL module is adapted to MAPPO rather than implemented as the original
DQN-based DIAL algorithm. The purpose is to test whether trainable
communication signals help the grouped actors coordinate.

中文：DIAL 风格模块测试的是另一种通信思路。
每个智能体先把自己的隐藏特征转换成消息。
训练时，消息保持连续并且可微，因此梯度可以通过通信通道传递。
测试时，消息可以被离散化。
在本项目中，DIAL 模块被改造成适用于 MAPPO 的形式，而不是完全复现原始基于 DQN 的 DIAL 算法。
这样做的目的是测试可训练的通信信号是否能帮助分组 actor 更好地协同。


\section{Completed Implementation}

At the midterm stage, the following implementation work has been completed:

中文：截至中期阶段，已经完成以下实现工作：


\begin{itemize}
  \item RBC notebook baseline for non-learning comparison;
  中文：用于非学习方法对比的 RBC notebook 基线；
  \item independent PPO and SAC training scripts;
  中文：独立 PPO 和 SAC 训练脚本；
  \item standard MAPPO training and testing scripts, where all buildings reuse
  the same actor network through parameter sharing;
  中文：标准 MAPPO 训练和测试脚本，其中所有建筑通过参数共享复用同一个 actor 网络；
  \item grouped MAPPO with K-means clustering and no communication;
  中文：带 K-means 聚类、但不使用通信的分组 MAPPO；
  \item intra-group communication variants, including no communication,
  CommNet-style mean communication, and PowerNet-style structured information
  passing;
  中文：组内通信变体，包括无通信、CommNet 风格均值通信，以及 PowerNet 风格结构化信息传递；
  \item GAT-style graph attention communication over a fixed building-similarity
  graph;
  中文：在固定建筑相似性图上运行的 GAT 风格图注意力通信；
  \item weighted same-group and other-group communication, where same-group
  messages are weighted more strongly than messages from the rest of the
  portfolio;
  中文：组内和组外加权通信，其中组内消息的权重大于来自其他建筑组的消息；
  \item attention and differentiable-message variants, including TarMAC-style
  attention and DIAL-style communication;
  中文：注意力和可微信息变体，包括 TarMAC 风格注意力和 DIAL 风格通信；
  \item shared centralized critic and shared value normalizer for grouped
  policies;
  中文：用于分组策略的共享集中式 critic 和共享 value normalizer；
  \item grouped replay buffer preserving communication structure during PPO
  updates;
  中文：在 PPO 更新过程中保留通信结构的分组 replay buffer；
  \item checkpoint loading and saving for test-only evaluation;
  中文：用于 test-only evaluation 的 checkpoint 加载与保存；
  \item Annex 96 metric export, daily metric tables, and plotting utilities,
  with cost and carbon fields marked as unavailable when the CE1 data provides
  no pricing or carbon-intensity signals.
  中文：Annex 96 指标导出、每日指标表格和绘图工具；当 CE1 数据没有提供电价或碳排放强度信号时，成本和碳排放字段会标记为不可用或不可解释。
\end{itemize}

The most important outcome so far is that the main methods can now be run and
compared through a common experimental interface. The completed Vermont
benchmark results show that communication changes the balance between tracking,
comfort, and secondary metrics. In the current selected benchmark set,
\texttt{mappo\_grouped\_powernet\_global} is the strongest overall
method. The updated ranking tables also show a more nuanced picture:
the default weighted communication variant is strongest on the primary-only
ranking, while TarMAC and weighted communication with
\mbox{$\alpha=0.90, \beta=0.10$} remain competitive all-round options.

中文：目前最重要的成果是，主要方法现在可以通过统一的实验接口运行和比较。
完整的 Vermont 基准结果说明，通信机制会改变负荷跟踪、舒适性和次级指标之间的取舍。
在当前选定的基准结果集中，
\texttt{mappo\_grouped\_powernet\_global} 是当前综合表现最强的方法。
更新后的排序表也说明，默认加权通信在主要指标上最强，而 TarMAC 和
\mbox{$\alpha=0.90, \beta=0.10$} 的加权通信仍然是比较均衡、很有竞争力的方案。


\section{Current Limitations}

Although the current method variants already show clear progress beyond the
basic baselines, this part of the project is still not finished. A useful
reference point is the independent PPO baseline. Its advantage is that it is
simple, stable, and easy to train and debug, and in the current results it
still remains competitive on some metrics. Its weakness is that each building
learns largely on its own, so it cannot model portfolio-level coordination
directly. In addition, PPO has a high training-time cost in multi-agent
settings, and this usually becomes more severe as the number of agents grows; in
comparison with shared-structure methods such as MAPPO, this is a major
disadvantage. The current grouped MAPPO variants show that moving beyond single
PPO is worthwhile, especially when the goal is to improve overall coordination
and to study communication design, but the comparison also shows that
independent PPO is not a trivial baseline and has not been dominated in every
metric.

中文：虽然当前这些方法变体相比基础基线已经有了比较明显的进展，但这一部分研究还没有完成。
一个很重要的参照是独立 PPO 基线。它的优点是结构简单、训练比较稳定，也比较容易调试，
而且在当前结果中它在部分指标上仍然有竞争力。
它的缺点是每栋建筑基本各学各的，无法直接建模组合层面的协同控制。
同时，PPO 在多智能体环境里的训练时间成本也很高，而且智能体数量越多，这个问题通常越明显；
和 MAPPO 这样的共享结构相比，这是一个很大的劣势。
目前的分组 MAPPO 变体说明，走出单个 PPO、研究多智能体协同和通信设计是值得的，
但结果也说明独立 PPO 并不是一个很弱的基线，它并没有在所有指标上都被完全超越。

Another limitation comes from the current K-means grouping and the TarMAC-style
attention mechanism. In the current implementation, K-means determines not only
the building groups but also the actor-sharing structure, while TarMAC only
learns dynamic attention weights within this fixed grouped-actor framework.
These two ideas are not fully aligned: hard grouping can simplify training and
control parameter size, but it may also restrict the attention module from
discovering more useful cross-group relationships. Therefore, the current
pipeline still does not answer which grouping structure is most suitable for
attention-based communication, or whether softer and learnable actor-assignment
mechanisms should be studied in the future.

中文：另一个限制来自当前 K-means 分组和 TarMAC 风格注意力机制之间。
在当前实现里，K-means 不仅决定了建筑分组，也决定了 actor 的共享结构；而 TarMAC 只是在这个固定分组 actor 框架内，根据当前隐藏特征动态学习注意力权重。
这两种思路并不完全一致：硬分组虽然能简化训练并控制参数规模，但也可能限制注意力模块发现更有用的跨组关系。
因此，当前流程还没有回答清楚：在已经固定 actor 分组的前提下，什么样的分组方式最适合和注意力通信结合，以及未来是否需要进一步研究软分组或可学习的 actor 分配机制。

There are also several model variants that have not yet been tested. One obvious
next step is a hybrid of TarMAC and PowerNet, so that the model can combine
structured message passing with learned attention instead of treating them as
separate alternatives. Another open direction is PPO-structure variation. The
current implementation mainly uses one centralized critic, but variants such as
multiple critics, group-specific critics, or mixed local-global critics may be
better suited to grouped communication settings, because multi-critic designs
may lead to more stable and more conservative parameter updates. These
architectural questions are still open \cite{maddpg2017,centralizedCritics2023}.

中文：此外，还有几类模型变体还没有测试。
一个直接的下一步是把 TarMAC 和 PowerNet 结合起来，让模型既有结构化消息传递，又能学习注意力权重，而不是把两者完全当成分开的方案。
另一个开放方向是 PPO 结构本身的变体。当前实现主要使用单个集中式 critic，
但多个 critic、组内 critic，或者局部与全局结合的 critic 结构，可能更适合这种分组通信设置，因为在参数更新上，多critic 可能会更稳定，更保守。
这些架构问题目前都还没有系统研究 \cite{maddpg2017,centralizedCritics2023}。

Finally, there are still some extension studies that have not yet been fully
completed. In particular, Texas RL training with the flatter district target
has not yet been included in the main comparison, and multi-seed validation is
also still pending.

中文：最后，还有一些尚未完善的扩展研究。
具体来说，平稳District Target 的 Texas RL 训练还没有纳入主对比，多随机种子验证也还没完成。
